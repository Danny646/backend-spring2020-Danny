//Fisrt Argument is what to run, second is how long to wait for in milestones 
function dpSetTimeout( instructions,timeoutLength, count = -1, pause = 0) {
   if (typeof  === timeoutLength ==="number" && typeof count === "number" &&
  typeof pause = = = "number") 
}

for (let i = 0; i <count; i++)  {
  let pauseLength = pause  *i;
}

return 0;
} else  {
   return 1; 
}
}

function mySpecialFunction() {
    console.log ("running something here");  
  } 
  dpSettiemout()